# Mind Orbit PRD (Sprint 6 MVP) — Final

문서 버전: v1.1  
작성일: 2026-04-13  
대상 릴리즈: Sprint 6 MVP (제출 마감 2026-04-16 19:00)

---

## 1) 제품 정의

### 1.1 제품 비전
- 가장 가볍고 직관적인 개인용 마인드맵 도구를 제공한다.
- "카톡 치듯 입력하고 연결하는" 빠른 사고 정리 경험을 만든다.

### 1.2 문제와 해결
- 기존 도구는 초기 로딩/기능 과밀/협업 중심으로 개인 사고 정리에 과하다.
- Mind Orbit은 로컬 전용(IndexedDB) + 모바일 우선 + 최소 UI로 진입장벽을 낮춘다.

### 1.3 타깃 사용자
- 개인 아이디어 정리, 기획 초안, 공부 노트 정리가 필요한 단일 사용자
- 모바일에서 즉시 메모하고 구조화하려는 사용자

### 1.4 성공 기준 (Sprint 6)
- 오프라인 환경에서 생성/편집/저장/재진입 복구가 100% 동작
- 모바일 주요 동작(탭/핀치/패닝/편집) 실패율 0건
- 신규 맵 생성 후 첫 노드 입력까지 10초 이내
- 필수 QA 항목 MUST 100% 통과

---

## 2) 범위 정의

### 2.1 In Scope (Sprint 6 MVP)
- 랜딩, 대시보드, 에디터, 저장 모달
- 노드/엣지 생성/수정/삭제
- 자동저장(디바운스) + 수동저장
- 모바일 터치 UX + PC 기본 단축키
- 기본 텍스트 스타일(B/I/U), 최소 Undo(최대 20단계)

### 2.2 Out of Scope (Sprint 7+)
- 실시간 협업, 클라우드 동기화
- 노드별 고급 스타일 커스터마이징
- 내보내기(PDF/이미지), 음성/AI 기능
- 고급 Undo/Redo, 썸네일 실렌더링, 템플릿/테마 확장

---

## 3) 플랫폼/기술 제약

### 3.1 기술 스택
- React (Vite)
- React Flow
- IndexedDB (`idb` 권장)
- Zustand (또는 Context)
- Tailwind CSS (다크 테마 고정)
- Lucide React, Pretendard

### 3.2 비기능 요구사항
- 서버 통신 없음 (완전 로컬)
- 첫 화면 체감 진입 2초 이내
- 모바일 우선 레이아웃(`max-width: 430px`, `100dvh`, safe-area 대응)
- 주요 조작 버튼 터치 영역 최소 44x44

### 3.3 호환 기준
- 모바일: iOS Safari 최신, Android Chrome 최신
- 데스크톱: Chrome 최신, Safari 최신

---

## 4) 정보 구조 및 라우팅

- `/` : 랜딩
- `/dashboard` : 맵 목록/생성/삭제
- `/editor/:mapId` : 편집

핵심 컴포넌트 트리:
- `LandingPage`
- `DashboardPage` (`NewMapCard`, `MindMapCard`)
- `EditorPage` (`EditorHeader`, `ReactFlowCanvas`, `BottomToolbar`, `EmptyState`)
- `SaveModal`

---

## 5) 데이터 모델

```ts
interface MindMap {
  id: string;          // uuid
  title: string;
  createdAt: number;
  updatedAt: number;
  nodes: Node[];
  edges: Edge[];
}
```

IndexedDB:
- DB: `mind-orbit-db` (v1)
- Store: `mindmaps` (`keyPath: id`)
- Index: `updatedAt`

정렬/조회:
- 대시보드는 `updatedAt desc`

---

## 6) 기능 요구사항 (Functional Requirements)

## FR-1 랜딩 페이지
- 앱명/카피/시작 CTA 제공
- CTA 클릭 시 `/dashboard` 이동
- 선택: `hasVisited` 기반 재방문 자동 이동

수용 기준:
- 첫 방문 사용자가 1탭으로 대시보드 진입 가능

## FR-2 대시보드
- 저장된 맵 목록 조회
- 신규 맵 생성, 기존 맵 열기, 맵 삭제, 공유(임시 방식)

상세:
- 신규 생성: 빈 맵 생성 후 즉시 에디터 이동
- 삭제: 즉시 삭제 + 2초 Undo 토스트(권장)
- 카드 정보: 제목, 상대시간(오늘/어제), 노드 수
- 썸네일: 더미 시각요소(실렌더링 아님)

수용 기준:
- 생성/삭제/재진입 시 목록 상태 일관성 유지

## FR-3 에디터
- 헤더: 홈, 제목 편집, 저장 버튼
- 캔버스: 노드/엣지 시각화 및 조작
- 툴바: 자식/형제 추가, B/I/U, 삭제
- Empty State: 노드 0개일 때 안내

노드 상호작용:
- 클릭: 선택
- 더블클릭(모바일은 더블탭 또는 길게 탭): 인라인 편집
- Tab: 자식 생성 + 자동 편집 진입
- Enter: 형제 생성 + 자동 편집 진입
- Delete/Backspace: 선택 노드 삭제(루트 제외)

삭제 규칙:
- 노드 삭제 시 하위 descendants 재귀 삭제

수용 기준:
- 키보드/터치 기반 주요 조작 모두 동등하게 동작

## FR-4 저장 모달
- 저장 버튼 클릭 시 모달 오픈
- 기존 제목 pre-fill
- Enter 저장, ESC 취소
- 저장 성공 시 헤더 제목 즉시 반영

## FR-5 저장 전략
- 자동저장: 변경 후 1.5초 debounce
- 수동저장: 즉시 저장
- 재진입 복구: 마지막 저장 상태 로드

오류 처리:
- IndexedDB 실패 시 사용자 토스트 노출
- 저장 실패 상태일 때 헤더에 경고 인디케이터 표시

## FR-6 최소 Undo
- 최대 20 스택 (nodes/edges/title 스냅샷)
- `Cmd/Ctrl+Z` 지원
- 자동저장 트리거와 Undo 충돌 방지(히스토리 병합 정책 적용)

---

## 7) UX/UI 요구사항

### 7.1 디자인 원칙
- 상주 주요 액션 버튼 5개 이하
- 시각적 강조는 보라 계열로 일관
- 편집 흐름 중 불필요 모달 최소화

### 7.2 모바일 UX
- 핀치 줌, 한 손가락 패닝, 탭 선택
- 키보드 오픈 시 하단 툴바가 입력 영역을 가리지 않아야 함
- `visualViewport` 이벤트로 툴바 위치 보정

### 7.3 접근성(최소 기준)
- 버튼/아이콘 `aria-label` 제공
- 포커스 스타일 유지
- 텍스트 대비 WCAG AA 최소 충족 목표

---

## 8) 공유 기능 정책 (MVP 현실화)

서버가 없으므로 "실시간 공유 URL"은 제공하지 않는다.

Sprint 6 구현안:
- 옵션 A(기본): 맵 JSON 복사(클립보드)
- 옵션 B(선택): 소형 맵에 한해 URL 인코딩 공유

문구 정책:
- 사용자에게 "기기 간 실시간 동기화 아님"을 명확히 표시

---

## 9) 예외/오류 시나리오

- IndexedDB 사용 불가(예: 프라이빗 모드 제약): 읽기 전용 안내 + 임시 메모 불가 토스트
- 저장 충돌(연속 입력 중 수동저장): 마지막 write wins, 실패 시 재시도 버튼
- 잘못된 맵 ID 접근: 에러 화면 + 대시보드 복귀 CTA
- 빈 제목 저장: 기본 제목 `제목 없는 마인드맵`

---

## 10) 분석 이벤트 (로컬 로그 기반)

MVP에서는 외부 분석 SDK 없이 콘솔/로컬 로그만 수집한다.

필수 이벤트:
- `map_create`
- `map_open`
- `node_add_child`
- `node_add_sibling`
- `node_delete`
- `map_save_auto`
- `map_save_manual`
- `save_error`

목적:
- 제출 전 사용성 검증 및 실패 포인트 확인

---

## 11) QA 체크리스트 (Definition of Done)

### MUST
- [ ] 모바일 브라우저 레이아웃 깨짐 없음
- [ ] 핀치 줌/터치 패닝 정상
- [ ] 터치로 노드 추가/편집/삭제 가능
- [ ] 오프라인 새로고침 후 데이터 유지
- [ ] PC에서 Tab/Enter로 트리 확장 가능
- [ ] `Cmd/Ctrl+S` 수동저장 동작
- [ ] 생성→저장→대시보드 재진입 시 목록 반영
- [ ] 삭제 후 목록에서 즉시 제거

### SHOULD
- [ ] 키보드 오픈 시 툴바 위치 보정
- [ ] 저장 상태 피드백(저장됨/실패)
- [ ] Empty State 정상 노출
- [ ] 카드에 노드 수 표시
- [ ] `Cmd/Ctrl+Z` 최소 Undo 동작

### NICE TO HAVE
- [ ] B/I/U 실시간 반영
- [ ] JSON 공유 복사
- [ ] 상대시간(오늘/어제) 포맷

---

## 12) 일정 및 게이트 (Sprint 6)

- D-3: 코어 편집(노드 CRUD, 저장) 완료
- D-2: 모바일 UX/단축키/오류처리 완료
- D-1: QA 리그레션 및 버그 픽스
- D-day(4/16 19:00): 데모 가능한 안정 빌드

릴리즈 게이트:
- MUST 전항목 통과
- 치명 버그(데이터 유실/편집 불가) 0건

---

## 13) 리스크 및 대응

- 모바일 브라우저 제스처 충돌  
  - 대응: React Flow 설정값 고정, 디바이스 실기 테스트 우선
- IndexedDB 환경별 제약  
  - 대응: 실패 토스트 + 기능 제한 안내 + 재시도
- 스코프 과대(공유/Undo 고도화)  
  - 대응: MVP 기준 엄격 적용, 고급 기능 Sprint 7 이관

---

## 14) 최종 의사결정 요약

- 서버 기능 전면 제외, 로컬 우선 전략 유지
- 공유는 "복사 기반"으로 한정
- 최소 Undo(20단계)는 Sprint 6에 포함
- 모바일 사용성/안정성을 기능 수보다 우선

